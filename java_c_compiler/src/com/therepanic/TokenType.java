package com.therepanic;

public enum TokenType {
    IDENTIFIER,
    NUMBER,
    PLUS,
    MINUS,
    ASSIGN, // =
    SEMICOLON,
    COMMA,
    IF,
    ELSE,
    WHILE,
    LPAREN,
    MUL,
    DIV,
    RPAREN,
    LBRACE,
    RBRACE,
    GT, // >
    LT, // <
    GTE, // >=
    LTE, // <=
    EQ, // ==
    NEQ, // !=
    INCREMENT,
    DECREMENT,
    PLUS_ASSIGN,
    MINUS_ASSIGN,
    RETURN,
    STRUCT,
    TYPEDEF
}
