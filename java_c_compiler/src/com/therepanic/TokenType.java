package com.therepanic;

public enum TokenType {
    IDENTIFIER,
    NUMBER,
    PLUS,
    MINUS,
    ASSIGN,
    SEMICOLON,
    IF,
    ELSE,
    WHILE,
    LPAREN,
    RPAREN,
    LBRACE,
    RBRACE,
    GT, // >
    LT, // <
    GTE, // >=
    LTE, // <=
    EQ, // ==
    NEQ // !=
}
